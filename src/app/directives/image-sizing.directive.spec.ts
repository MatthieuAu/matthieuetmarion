import { ImageSizingDirective } from './image-sizing.directive';

describe('ImageSizingDirective', () => {
  it('should create an instance', () => {
    const directive = new ImageSizingDirective();
    expect(directive).toBeTruthy();
  });
});
